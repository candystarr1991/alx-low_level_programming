#include <stdio.h>

/**
 * main - Prints the alphabet.
 *
 * Return: 0 Always (Success)
 */
int main(void)
{
	int alp;

	for (alp = "a"; alp <= "z"; alp++)
	{
		putchar(alp);
	}
	putchar(\n);
	return (0);
}
